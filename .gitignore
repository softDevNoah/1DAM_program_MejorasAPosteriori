# Archivos por extensión en cualquier subdirectorio
*.class
*.dat
*.metadata

# Archivos específicos de configuración (recursivos, para cualquier proyecto)
.classpath
desktop.ini

# Carpetas completas en cualquier nivel (sin la barra inicial, importante)
bin/
metadata/
.settings/
